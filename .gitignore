# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Derived data (regenerable) — see PLATFORM-SPEC.md §4.1
data/
backtests/
market-data/
*.duckdb
*.duckdb.tmp
*.duckdb.wal
research_cache/

# Legacy local Postgres cluster (removed in Phase 0; ignore leftovers)
backend/.pgdata/
backend/*.log

# Local secrets — API keys, DB URLs. See backend/.env.example.
.env

# OS / editor
.DS_Store
